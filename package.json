{
  "name": "security-principles-vault-demo",
  "version": "1.0.0",
  "description": "Intentionally vulnerable Ethereum vault demo for a Security Principles course.",
  "main": "index.js",
  "scripts": {
    "test": "npx hardhat test",
    "deploy:local": "npx hardhat run scripts/deploy.js --network hardhat",
    "deploy:sepolia": "npx hardhat run scripts/deploy.js --network sepolia",
    "demo:local": "npx hardhat run scripts/demo.js --network hardhat",
    "demo:sepolia": "npx hardhat run scripts/demo.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.0.0",
    "hardhat": "^2.22.0"
  }
}
